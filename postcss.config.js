// Object form (not the string-array form Next tolerated) so both Vite's and
// Next's PostCSS loaders accept it during the migration window.
module.exports = {
	plugins: {
		tailwindcss: {},
		autoprefixer: {}
	}
};
